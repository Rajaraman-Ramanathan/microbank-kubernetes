name: microbank-ci-dev

on:
    workflow_dispatch:

env:
  REGISTRY: docker.io
  IMAGE_NAMESPACE: ${{ vars.DOCKERHUB_USERNAME }}
  DOCKER_BUILDKIT: 1

jobs:
    build-and-push:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout code
          uses: actions/checkout@v4
        - name: Set up JDK 21
          uses: actions/setup-java@v4
          with:
            distribution: temurin
            java-version: 21
            cache: maven
        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v3
        - name: Login to Docker Hub
          uses: docker/login-action@v3
          with:
            username: ${{ vars.DOCKER_USERNAME }}
            password: ${{ secrets.DOCKER_PASSWORD }}
        - name: Build and push Docker image
          uses: docker/build-push-action@v5
          with:
            context: ./account
            file: ./account/Dockerfile
            push: true
            tags: |
              ${{ vars.DOCKER_USERNAME }}/account-service:1.0.2
              ${{ vars.DOCKER_USERNAME }}/account-service:${{ github.sha }}
            cache-from: type=registry,ref=${{ vars.DOCKER_USERNAME }}/account-service:buildcache
            cache-to: type=registry,ref=${{ vars.DOCKER_USERNAME }}/account-service:buildcache,mode=max
    
