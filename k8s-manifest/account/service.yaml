apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: microbank-dev
  labels:
    app: account-service
spec:
  type: ClusterIP
  selector:
    app: account-service
  ports:
  - name: http
    port: 80
    targetPort: 8082
