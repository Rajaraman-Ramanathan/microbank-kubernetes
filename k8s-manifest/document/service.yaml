apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: microbank-dev
  labels:
    app: document-service
spec:
  type: ClusterIP
  selector:
    app: document-service
  ports:
  - name: http
    port: 80
    targetPort: 8084
