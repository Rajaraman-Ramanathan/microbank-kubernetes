apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: microbank-dev
  labels:
    app: keycloak
spec:
  type: ClusterIP
  selector:
    app: keycloak
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
